{
  "name": "learnify",
  "version": "1.0.0",
  "private": true,
  "description": "Learnify — the latest tech, science & learning, browsable by keyword and source. Making learning addictive.",
  "type": "module",
  "scripts": {
    "scrape": "python3 scrape.py",
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}
